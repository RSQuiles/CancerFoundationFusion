{
  "experiment_name": "paired_mix",
  "output_dir": "ablation_runs/paired_mix",
  "dry_run": false,
  "train_entrypoint": "pretrain:main",
  "base_config": {
    "trainer": {
      "gpus": 1,
      "num_workers": 4,
      "precision": "bf16-mixed",
      "strategy": "auto",
      "epochs": 5,
      "seed": 0,
      "val_check_interval": 0.5,
      "log_interval": 50,
      "save_dir": "${work}/outputs/save_CFF/ablation_paired_mix",
      "wandb_entity": "rquiles",
      "wandb": "ablation_paired_mix",
      "compile": false,
      "ckpt_every_n_steps": 10000,
      "resume_from_checkpoint": "last"
    },
    "data": {
      "train_path": "/cluster/work/boeva/rquiles/data/paired_dataset_counts/pipeline_ready",
      "conditions": ["tissue_general", "assay", "paired"],
      "zero_percentages": [0.2, 0.4, 0.6],
      "bulk_ratio": 0.3,
      "pb_ratio": 0.3,
      "n_sc_per_pseudobulk": 25,
      "pb_group_column": "tissue_general",
      "trunc_by_sample": true,
      "balanced_sampler": true,
      "balanced_labels": ["tissue_general", "assay"]
    },
    "model": {
      "nlayers": 6,
      "nheads": 8,
      "embsize": 128,
      "d_hid": 256,
      "lr": 0.0001,
      "warmup_ratio_or_step": 10000,
      "max_seq_len": 600,
      "batch_size": 16,
      "input_style": "binned",
      "input_data": "counts",
      "loss": "mse",
      "training_tasks": "both",
      "do_mvc": true,
      "where_condition": "none",
      "gen_method": "theirs"
    },
    "features": {
      "loss_weight_paired": 10,
      "loss_weight_dat": 10,
      "loss_weight_agg": 1,
      "loss_weight_reconstruction": 50
    }
  },
  "base_overrides": {},
  "ablations": [
    {
      "name": "unified",
      "overrides": {
        "trainer": {
          "save_dir": "${work}/outputs/save_CFF/ablation_paired_mix/unified",
          "wandb_name": "unified"
        },
        "data": {},
        "model": {},
        "features": {
          "unified": true,
	        "paired_sampling": true
        }
      }
    },
        {
      "name": "dat_agg",
      "overrides": {
        "trainer": {
          "save_dir": "${work}/outputs/save_CFF/ablation_paired_mix/dat_agg",
          "wandb_name": "dat_agg"
        },
        "data": {},
        "model": {},
        "features": {
          "unified": true,
	        "paired_sampling": true,
          "do_dat": true,
          "dat_columns": ["modality", "assay"],
          "agg_consistency": true,
          "agg_fn": "mean"
        }
      }
    }
  ],
  "downstream_tasks": [],
  "slurm": {
    "enabled": true,
    "python_executable": "python",
    "sbatch_args": [
      "--time=48:00:00",
      "--partition=gpu",
      "--ntasks-per-node=1",
      "--gres=gpu:rtx4090:1",
      "--cpus-per-task=4",
      "--mem-per-cpu=16G"
    ],
    "environment": {},
    "job_name_prefix": "ab_paired"
  }
}